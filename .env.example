# ========================================
# Docker 이미지 설정
# ========================================
# 프로덕션: GHCR 이미지 사용 (YOUR_GITHUB_USERNAME을 실제 GitHub 사용자명으로 변경)
# DOCKER_IMAGE=ghcr.io/YOUR_GITHUB_USERNAME/blog-api:latest
# 로컬 개발: docker-compose.dev.yml 사용 (주석 유지)

# ========================================
# 애플리케이션 기본 설정
# ========================================
APP_PORT=80
# 서버 배포 주소 (서버의 공인 IP 또는 도메인)
PUBLISHED_URL=your-server-ip-or-domain

# 환경 설정 (local 또는 prod로 변경)
# local: 로컬 개발 환경
# prod: 프로덕션 배포 환경
SPRING_PROFILES_ACTIVE=prod

# ========================================
# URL 설정 (환경에 따라 변경)
# ========================================
# 프로덕션 배포: PUBLISHED_URL 사용
FRONTEND_URL=http://your-server-ip-or-domain
BACKEND_URL=http://your-server-ip-or-domain

# CORS 허용 도메인 (쉼표로 구분)
# 프로덕션: 배포 서버 주소 + 로컬 개발 주소
CORS_ALLOWED_ORIGINS=http://your-server-ip-or-domain,http://localhost:3000,http://localhost:8080

# 로컬 개발시에는 위 설정을 주석처리하고 아래 주석을 해제:
# APP_PORT=8080
# PUBLISHED_URL=localhost
# FRONTEND_URL=http://localhost:8080
# BACKEND_URL=http://localhost:8080
# CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:8080
# SPRING_PROFILES_ACTIVE=local

# ========================================
# MySQL Database
# ========================================
# MySQL Host
# - Docker Compose 사용시: 'mysql' (서비스 이름)
# - 로컬 설치시: 'localhost'
# - 프로덕션: RDS 엔드포인트 등
MYSQL_HOST=localhost

MYSQL_ROOT_PASSWORD=your_root_password
MYSQL_DATABASE=blog
MYSQL_USER=app
MYSQL_PASSWORD=your_app_password
MYSQL_PORT=3306

# ========================================
# 참고: Docker Compose 환경에서는
# MYSQL_HOST와 REDIS_HOST가 자동으로
# 서비스 이름(mysql, redis)으로 오버라이드됩니다
# ========================================

# ========================================
# JWT
# ========================================
# 최소 32바이트 이상의 안전한 키 사용 필수
JWT_SECRET=your-super-secret-jwt-key-at-least-32-bytes-long-change-this

# Access Token 만료 시간 (초) - 기본: 900 (15분)
JWT_ACCESS_EXP=900

# Refresh Token 만료 시간 (초) - 기본: 1209600 (14일)
JWT_REFRESH_EXP=1209600

# ========================================
# Redis
# ========================================
# Redis Host
# - Docker Compose 사용시: 'redis' (서비스 이름)
# - 로컬 설치시: 'localhost'
# - 프로덕션: ElastiCache 엔드포인트 등
REDIS_HOST=localhost
REDIS_PORT=6379

# ========================================
# Firebase 설정 (백엔드)
# ========================================
# Firebase Console > Project Settings > Service Accounts
# "Generate new private key" 버튼 클릭하여 JSON 파일 다운로드
# 다운로드한 파일을 src/main/resources/firebase-service-account.json에 저장

# Firebase 프로젝트 ID
FIREBASE_PROJECT_ID=your-project-id

# Firebase Admin SDK 서비스 계정 키 파일 경로
# 로컬: src/main/resources/firebase-service-account.json
# Docker: /app/firebase-service-account.json (docker-compose.yml 기본값 사용)
# 로컬 개발 시에는 아래 주석을 해제하고 사용:
# FIREBASE_SERVICE_ACCOUNT_PATH=src/main/resources/firebase-service-account.json

# ========================================
# Firebase 설정 (프론트엔드)
# ========================================
# Firebase Console > Project Settings > General 에서 확인 가능
REACT_APP_FIREBASE_API_KEY=your-api-key
REACT_APP_FIREBASE_AUTH_DOMAIN=your-project-id.firebaseapp.com
REACT_APP_FIREBASE_PROJECT_ID=your-project-id
REACT_APP_FIREBASE_STORAGE_BUCKET=your-project-id.appspot.com
REACT_APP_FIREBASE_MESSAGING_SENDER_ID=your-sender-id
REACT_APP_FIREBASE_APP_ID=your-app-id

# ========================================
# Kakao 설정
# ========================================
# Kakao Developers에서 발급
# https://developers.kakao.com/console/app
# 백엔드: REST API 키
KAKAO_REST_API_KEY=your-kakao-rest-api-key

# 프론트엔드: JavaScript 키
REACT_APP_KAKAO_JAVASCRIPT_KEY=your-kakao-javascript-key
